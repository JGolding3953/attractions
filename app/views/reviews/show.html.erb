<p id="notice"><%= notice %></p>

<%= link_to '< Back', polymorphic_path([@parent, @reviews]) %>

<p>
  <strong>Attraction:</strong>
  <%= @review.attraction_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @review.user_id %>
</p>

<p>
  <strong>Comments:</strong>
  <%= @review.comments %>
</p>

<p>
  <strong>Rating:</strong>
  <%= @review.rating %>
</p>

<a href="<%= edit_polymorphic_path([@parent, @review]) %>" class="button [small]">edit</a><br />
